package juno.model.card;

import juno.model.player.AbstractPlayer;

public class FirstPlayerManager implements AbstractFirstPlayerManager<AbstractPlayer> {
    @Override
    public AbstractPlayer firstPlayer(AbstractPlayer... cards) {
        return null;
    }
}
